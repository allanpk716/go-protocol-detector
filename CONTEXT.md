# go-protocol-detector

多协议端口扫描与协议识别工具。本词表固定"扫描结果"与"输出模式"两类术语，避免"失败"一词多义。

## Language

### 扫描结果

**命中 (Hit)**:
端口开放且协议检测匹配成功的目标。是数据。
_Avoid_: 成功（与"扫描成功完成"混淆）

**负结果 (Negative Result)**:
端口关闭或协议不匹配的目标。是扫描的正常产出，不是错误。
_Avoid_: 失败（与错误混淆）

**错误 (Error)**:
扫描动作本身未能完成，如参数非法、网络不可达。需要调用方处理。
_Avoid_: 失败（与负结果混淆）

**空结果成功 (Empty Success)**:
扫描正常完成但零命中。属于成功，不是"未找到"错误。

**Banner (banner)**:
命中目标返回的原始响应的截断头部片段（如 `SSH-2.0-OpenSSH_8.9`），用于识别服务类型与版本。

**负结果原因 (Negative Reason)**:
负结果的固定五分类之一：`closed`（连接被拒）、`timeout`（超时无响应）、`protocol_mismatch`（TCP 通但协议特征不符）、`unreachable`（网络层不可达）、`unknown`（兜底）。
_Avoid_: 失败原因（与错误混淆）

### 输出模式

**Agent 模式 (Agent Mode)**:
输出面向程序消费的模式，产出结构化信封流。
_Avoid_: 机器模式、JSON 模式

**人类模式 (Human Mode)**:
输出面向终端阅读的模式。
_Avoid_: 普通模式（"默认"归属哪个模式是独立决策，不用词面绑定）
